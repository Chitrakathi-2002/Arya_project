<?php
namespace App\Core;

class Upload {
    public static function handle(array $file, string $destinationDir, array $allowedTypes = ['image/jpeg', 'image/png', 'image/webp']): ?string {
        // Secure file upload handler
        return null;
    }
}
